const usersData = require("./users");
const listsData = require("./lists");
const cardsData = require("./cards");

module.exports = {
    users: usersData,
    lists: listsData,
    cards: cardsData
};